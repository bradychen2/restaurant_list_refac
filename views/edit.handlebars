<h1 class="mb-1 restaurant-show-title">請編輯餐廳詳細資訊</h1>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-md-6 mx-auto">
      <form action="/restaurants/{{restaurant._id}}/?_method=PUT" method="POST" id="edit">
        <p class="mb-1">
          <span class="text-secondary">
            餐廳名稱：
          </span>
          <input type="text" name="name" size="50" value={{restaurant.name}}>
        </p>

        <p class="my-3">
          <span class="text-secondary">
            英文名稱：
          </span>
          <input type="text" name="name_en" size="50" value={{restaurant.name_en}}>
        </p>

        <p class="my-3">
          <span class="text-secondary">
            類別：
          </span>
          <input type="text" name="category" size="50" value={{restaurant.category}}>
        </p>

        <p class="my-3">
          <span class="text-secondary">
            地址：
          </span>
          <input type="text" name="location" size="50" value={{restaurant.location}}>
        </p>

        <p class="my-3">
          <span class="text-secondary">
            電話：
          </span>
          <input type="text" name="phone" size="50" value={{restaurant.phone}}>
        </p>

        <p class="my-3">
          <span class="text-secondary">
            Google Map 網址：
          </span>
          <input type="url" name="google_map" size="50" value={{restaurant.google_map}}>
        </p>

        <p class="my-3">
          <span class="text-secondary">
            評價：
          </span>
          <input type="number" max="5" min="0" name="rating" size="50" step=".1" value={{restaurant.rating}}>
        </p>

        <p class="my-3">
          <span class="text-secondary">
            圖片網址：
          </span>
          <input type="url" name="image" size="50" value={{restaurant.image}}>
        </p>

        <p class="mt-3">
          <span class="text-secondary">
            詳細介紹：
          </span>
          <textarea rows="10" cols="50" form="edit" placeholder="餐廳詳細介紹"
            name="description">{{restaurant.description}}</textarea>
        </p>
        <p>
          <button class="btn btn-primary" type="submit">修改</button>
          <a href="/" class="ml-3">返回</a>
        </p>
      </form>
    </div>
  </div>
</div>